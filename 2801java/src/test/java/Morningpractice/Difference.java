package Morningpractice;

public class Difference {
	
	// Differences between Abstract and Interface
	// Abstract: 1)abstract is a class. 
	//           2) before the class we have to use abstract keyword.
	//           3)abstract allows both method.regular method and abstract method and which hide the body.
	//           4)in abstract method u have to declare abstract keyword before the method name  
	//           5)abstract method syntax: public abstract void method();
	//           6) in abstract if we want to implement or access then use extends keyword
	//           7) we can use public, default(void)and protected method in abstract class.
	//           8)in abstract, if you implement the default method will remain default.
	// Interface: 1) interface is not a class. Interface is a interface. 
	//            2)in interface we have to use interface keyword .like public interface
	//            3)interface allows only interface method which hide the body.
	//            4) in interface method u don't have to declare interface keyword before the method name
	//            5) interface method syntax: public void method(); 
	//            6) in interface if we want to implement or access then we use implements keyword
	//            7) we can use only public and default(void)method not use protected method in interface.
	//            8) in interface,if you implement the default method will become the public method.
	
	
	// similarities of abstract and interface method
	//            1)both serve the same purpose like implement the unimplemented methods 
	//            2)override happens in both methods
	//             3)both will keep method without body. 
	
	
	
	
	

	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}
